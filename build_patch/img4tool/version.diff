diff --git a/configure.ac b/configure.ac
index 454f70f..3306479 100644
--- a/configure.ac
+++ b/configure.ac
@@ -1,5 +1,5 @@
 AC_PREREQ([2.69])
-AC_INIT([img4tool], m4_esyscmd([git rev-list --count HEAD | tr -d '\n']), [tihmstar@gmail.com])
+AC_INIT([img4tool], m4_esyscmd([printf "197" | tr -d '\n']), [tihmstar@gmail.com])
 
 AC_CANONICAL_SYSTEM
 AC_CANONICAL_HOST
@@ -10,10 +10,10 @@ AC_CONFIG_HEADERS([config.h])
 AC_CONFIG_MACRO_DIRS([m4])
 
 
-AC_DEFINE([VERSION_COMMIT_COUNT], "m4_esyscmd([git rev-list --count HEAD | tr -d '\n'])", [Git commit count])
-AC_DEFINE([VERSION_COMMIT_SHA], "m4_esyscmd([git rev-parse HEAD | tr -d '\n'])", [Git commit sha])
-AC_SUBST([VERSION_COMMIT_COUNT], ["m4_esyscmd([git rev-list --count HEAD | tr -d '\n'])"])
-AC_SUBST([VERSION_COMMIT_SHA], ["m4_esyscmd([git rev-parse HEAD | tr -d '\n'])"])
+AC_DEFINE([VERSION_COMMIT_COUNT], "m4_esyscmd([echo "197" | tr -d '\n'])", [Git commit count])
+AC_DEFINE([VERSION_COMMIT_SHA], "m4_esyscmd([echo "aca6cf005c94caf135023263cbb5c61a0081804f" | tr -d '\n'])", [Git commit sha])
+AC_SUBST([VERSION_COMMIT_COUNT], ["m4_esyscmd([echo "197" | tr -d '\n'])"])
+AC_SUBST([VERSION_COMMIT_SHA], ["m4_esyscmd([echo "aca6cf005c94caf135023263cbb5c61a0081804f" | tr -d '\n'])"])
 
 
 # Checks for programs.
