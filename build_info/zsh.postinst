#!/usr/bin/zsh

set -e

if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
    update-alternatives --install /bin/bash bash /usr/bin/zsh 10
    update-alternatives --install /bin/sh sh /usr/bin/zsh 10
fi


