#!/bin/sh

set -e

if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
    update-alternatives --install /bin/bash bash /usr/bin/bash 100
    update-alternatives --install /bin/sh sh /usr/bin/bash 100
fi


